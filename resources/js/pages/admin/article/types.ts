export type ArticleForm = {
    id?: number;
    name: string;
    description?: string;
};

export type ArticleFormErrors = {
    name?: string;
    description?: string;
};

// export type ArticlePagination = {};
