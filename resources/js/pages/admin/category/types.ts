export type CategoryForm = {
    id?: number;
    name: string;
    description?: string;
};

export type CategoryFormErrors = {
    name?: string;
    description?: string;
};

// export type CategoryPagination = {};
